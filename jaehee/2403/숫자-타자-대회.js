function solution(numbers) {
  let answer = 0;
  let l = 4;
  let r = 6;

  for (let number of numbers.split("")) {
    const target = number.toString();
  }

  return answer;
}

// DP로 풀어야 할 것 같은데..!
